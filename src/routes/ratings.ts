import { Router } from "express"

const router = Router()

/* rate
req: {
    userId: number,
    mediaId: number,
    score: string
}

res: {
    userId: number,
    mediaId: number,
    score: string
}
*/
router.post('/')

export default router